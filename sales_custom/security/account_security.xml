<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <delete model="res.group" id="account.group_account_invoice"/>
    <delete model="res.group" id="account.group_account_manager"/>
<!--
    sales team leader access only its team calender
    <delete model="ir.rule" id="calendar_event_rule_employee"/>

   sales team leader access only its team calender
     <record id="group_account_invoice" model="res.groups">
        <field name="name">Billing</field>
        <field name="category_id" ref="base.module_category_accounting_and_finance"/>
        <field name="implied_ids" eval="[(4, ref('base.group_user'))]"/>
    </record>

    sales team leader access all sales calendar
   <record id="group_account_manager" model="res.groups">
        <field name="name">Billing Manager</field>
        <field name="category_id" ref="base.module_category_accounting_and_finance"/>
        <field name="implied_ids" eval="[(4, ref('group_account_invoice'))]"/>
    </record>-->


<!--make the lead menu appear only for the team leader

        <record model="res.groups" id="account.group_account_invoice">
            <field name="name">Billing1</field>
            <field name="implied_ids" eval="[(4, ref('sales_team.group_sale_salesman'))]"/>
        </record>


make the lead menu appear only for the team leader
        <record model="res.groups" id="account.group_account_manager">
            <field name="name">Billing Manager2</field>
            <field name="implied_ids" eval="[(4, ref('sales_team.sales_team.group_sale_manager'))]"/>
        </record>-->

</odoo>
